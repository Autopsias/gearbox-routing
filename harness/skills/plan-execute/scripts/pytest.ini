; Keep pytest's cache out of the skill directory (PYTHONPYCACHEPREFIX in
; ~/.claude/settings.json handles __pycache__; this handles .pytest_cache).
[pytest]
cache_dir = /tmp/pytest-cache/plan-execute-scripts
